{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Example",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/rotation_vector_example",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "build" // Assuming a build task named "build" exists or will be created
        }
    ]
}
